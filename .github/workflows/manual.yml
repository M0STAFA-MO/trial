on:
  workflow_dispatch:
     inputs:
         enviroments:
           description: 'chose enviroment'
           required: true
           default: 'staging'
           type: choice
           options:
             - staging
             - production


jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
          - name: Deploy appliction 
            run: echo 'Deploy the application'
